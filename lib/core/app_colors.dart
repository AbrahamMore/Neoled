import 'package:flutter/material.dart';

class AppColors {
  // Colores de los bordes navegaroes
  static const Color primary = Color(0xFFFFEB3B);
  static const Color secondary = Color(0xFF000000);
  static const Color accent = Color(0xFFFFFFFF);

  // Botones de el menu de inicio
  static const Color azul = Color(0xFF2196F3);
  static const Color verde = Color(0xFF4CAF50);
  static const Color rojo = Color(0xFFF44336);
  static const Color morado = Color.fromARGB(255, 189, 87, 207);
}
